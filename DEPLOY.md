# Deploying VEIL

Frontend on Vercel, backend on Render. They are separate deployments and the
**order matters** — the frontend bakes the API URL in at build time, so the
backend has to exist first.

---

## 1. MongoDB Atlas

The database must be Atlas, not a self-hosted `mongod`: `$vectorSearch` is an
Atlas feature and RAG stays disabled without it. The boot log says so explicitly
if it is missing.

1. **Network Access → Add IP Address → Allow access from anywhere** (`0.0.0.0/0`).
   Render's free tier has no static outbound IP, so an allowlist cannot work.
2. **Database Access** — create a user, and percent-encode any of `@ : / ? # [ ] %`
   in the password before putting it in the connection string.
3. Create the vector indexes once the backend is up (step 2.4).

## 2. Backend → Render

### 2.1 Create the service

Render dashboard → **New → Blueprint** → point at this repo. It reads
[`render.yaml`](render.yaml), which sets `rootDir: backend`, the build and start
commands, and `/health` as the health check.

### 2.2 Set the secrets

`render.yaml` marks these `sync: false`, meaning Render will not read them from
the file — set them in **Environment** on the service:

| Variable | Notes |
|---|---|
| `MONGODB_URI` | Atlas connection string |
| `GROK_API_KEY` | Groq — turn analysis, fallacies, AI opponent, sparring |
| `HUGGINGFACE_API_KEY` | `all-MiniLM-L6-v2` embeddings (384-d) |
| `FRONTEND_URL` | Your Vercel URL. Comma-separated for several. |
| `PERSPECTIVE_API_KEY` | Optional — toxicity falls back to the LLM, then keywords |

`JWT_SECRET` and `SLICK_ENCRYPTION_KEY` are generated by Render on first deploy.
Rotating either later invalidates every session / anonymous-feedback mapping.

`NODE_ENV`, `JWT_EXPIRE` and `AI_BUDGET_ENFORCED` are set in the blueprint and
should not be overridden — see *Things that will bite you* below.

### 2.3 Deploy and check

```
curl https://<your-service>.onrender.com/health
```

Should report `status: OK` plus the persona scheduler, feed scheduler and job
worker all running. All three run in-process, which is why this is a web service
rather than a serverless function.

### 2.4 Create the Atlas vector indexes

Once, from the Render **Shell** tab (or locally with production `MONGODB_URI`):

```
node scripts/createVectorIndexes.js
```

Creates the vector indexes *and* the filter paths that memory retrieval needs.
Re-running is a no-op. Without this, retrieval returns zero results and the app
still reports RAG as enabled — it fails silently.

Then verify the whole AI path end to end:

```
npm run smoke            # 18 checks against real models
npm run smoke:endpoints  # 27 endpoints
```

## 3. Frontend → Vercel

Existing project. Root Directory must be `frontend`.

Add one environment variable and **redeploy** — CRA inlines `REACT_APP_*` at
build time, so changing it without rebuilding does nothing:

```
REACT_APP_API_URL = https://<your-service>.onrender.com
```

No trailing slash. The code appends `/api` itself.

## 4. Close the CORS loop

Add the Vercel domain to `FRONTEND_URL` on Render (step 2.2). The known Vercel
domains are already hardcoded in [`src/config/cors.js`](backend/src/config/cors.js),
which both the HTTP middleware and the Socket.io handshake read — they drifted
apart once and silently broke websockets.

---

## Things that will bite you

**`NODE_ENV=production` is load-bearing.** The refresh-token cookie only gets
`secure: true` and `sameSite: 'none'` when it is set, and a cross-site cookie
from Vercel to Render needs both. Without it, login appears to work and then
every token refresh fails.

**Render's free tier sleeps after 15 minutes idle.** While asleep the job worker
and both schedulers stop, and the next request takes ~50s to cold-start. Fine for
a demo, not for anything with real users — the $7/mo Starter plan removes it.

**Groq's free tier is 100k tokens/day, shared across every user.** This is the
real ceiling. A sparring session costs 4–8 model calls, so the platform supports
roughly 15–25 sessions a day in total. `AI_BUDGET_ENFORCED=true` caps per-user
spend so one person cannot drain it. When the 70B model's daily cap is reached,
`grokService` downgrades to the 8B model automatically rather than failing —
expect fallacy detection to get noticeably worse when that happens.

**Smoke checks that need the smart model report as skipped, not passed.** The
fallacy pair and the AI-opponent pair pass or fail at random on the 8B fallback
(measured 0/6 one day, 3/3 the next). Rather than colouring the run red for an
upstream quota, `npm run smoke` reports them as skipped with the model that
actually served, and excludes them from the pass count:

```
14/14 checks passed, 4 skipped

Skipped (not asserted — do not read as passing):
  ⏭️  Stays silent on a sound argument — downgraded to llama-3.1-8b-instant
```

A skipped run proves nothing about those paths. Re-run once the daily quota
resets to get a real result — and treat a run with skips as incomplete, not green.

**Atlas requires `0.0.0.0/0`.** Render free has no static outbound IP.
